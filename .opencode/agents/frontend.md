# frontend subagent

React/TypeScript/Tailwind frontend specialist for BookPrice.

Build UI components, pages, styling, and frontend logic.

Conventions:
- Functional components with hooks
- TypeScript strictly typed
- Tailwind CSS for styling
- Vite as build tool
